# Streamlit Cloud / GitHub Actions runtime (Python 3.11+ there; local venv
# pins streamlit<1.36 separately for the system Python 3.9)
streamlit>=1.35
pandas>=2.0
duckdb>=1.0
pyyaml>=6.0
psycopg2-binary>=2.9
pyarrow>=15.0
